import Link from "next/link";
import { SiteFooter, SiteHeader } from "../components/SiteChrome";
import { products } from "../components/product-data";

export default function TreatmentsPage(){return <main className="detail-page"><SiteHeader/><section className="collection-hero"><div><p className="detail-kicker">Treatment options</p><h1>Prescription weight care, with choices.</h1><p>Compare branded and compounded options available for independent provider review through Rejuvonix.</p><Link className="detail-primary" href="/get-started">Find my options</Link></div><img src="/rejuvonix-lifestyle.png" alt="Adults preparing a healthy meal together"/></section><section className="collection-section"><div className="compare-heading"><p className="detail-kicker">Explore treatments</p><h2>Start with the format that fits your life.</h2><p>A provider makes the final clinical decision after reviewing your assessment.</p></div><div className="collection-grid">{products.map(product=><article key={product.slug}><div className="collection-product"><img src={product.image} alt={`${product.name} product packaging`}/></div><p>{product.status}</p><h2>{product.name}</h2><dl><dt>Ingredient</dt><dd>{product.ingredient}</dd><dt>Format</dt><dd>{product.format}</dd><dt>Schedule</dt><dd>{product.cadence}</dd></dl><Link href={`/treatments/${product.slug}`}>View treatment →</Link></article>)}</div></section><section className="collection-life"><img src="/rejuvonix-movement.png" alt="Woman walking outdoors"/><div><p className="detail-kicker">Built around you</p><h2>Medication is one part of a complete plan.</h2><p>Rejuvonix brings provider access, fulfillment updates, progress check-ins and practical lifestyle guidance together.</p><Link className="detail-primary" href="/support">See what is included</Link></div></section><SiteFooter/></main>}
