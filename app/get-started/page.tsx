"use client";
import { useState } from "react";
import Link from "next/link";
import { SiteHeader } from "../components/SiteChrome";
const questions=[{title:"What would you most like help with?",options:["Lose weight","Manage appetite","Improve metabolic health","Maintain progress"]},{title:"Which treatment format do you prefer?",options:["Weekly injection","Daily pill","Open to either","Let a provider help me decide"]},{title:"How would you like to continue?",options:["Complete my health assessment","Compare treatments first","Learn how the program works"]}];
export default function StartPage(){const [step,setStep]=useState(0);const [done,setDone]=useState(false);return <main className="detail-page start-page"><SiteHeader/><section className="start-shell"><div className="start-visual"><img src="/rejuvonix-get-started-group.png" alt="A diverse group of adults smiling with confidence"/><div><p className="detail-kicker">Start here</p><h1>A few questions can help organize your next step.</h1></div></div><div className="start-panel">{!done?<><div className="start-progress"><span style={{width:`${((step+1)/questions.length)*100}%`}}></span></div><p className="detail-kicker">Question {step+1} of {questions.length}</p><h2>{questions[step].title}</h2><div className="start-options">{questions[step].options.map(option=><button key={option} onClick={()=>step===questions.length-1?setDone(true):setStep(step+1)}>{option}<span>→</span></button>)}</div>{step>0&&<button className="start-back" onClick={()=>setStep(step-1)}>← Back</button>}</>:<div className="start-result"><span>✓</span><p className="detail-kicker">Next step</p><h2>Complete your secure health assessment.</h2><p>A licensed provider will review your information and decide whether treatment is appropriate.</p><a className="detail-primary" href="mailto:care@rejuvonix.com?subject=Begin%20health%20assessment">Continue securely</a><Link className="detail-secondary" href="/treatments">Compare treatments</Link><small>A prescription is not guaranteed.</small></div>}</div></section></main>}
